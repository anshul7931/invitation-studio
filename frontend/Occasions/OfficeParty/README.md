# Office Party invitation

Edit `office.js` to change Office Party-only form sections, labels, theme options, generated card details, and document title.

Backend defaults and required fields for API/Postman calls still live in `server/occasion-schema.js` under the `office` key.
