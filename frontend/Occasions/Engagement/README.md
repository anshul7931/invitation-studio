# Engagement invitation

Edit `engagement.js` to change Engagement-only form sections, labels, theme options, generated card details, and document title.

Backend defaults and required fields for API/Postman calls still live in `backend/occasion-schema.js` under the `engagement` key.
